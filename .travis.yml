language: android
jdk: oraclejdk8
sudo: false

branches:
  only:
    - master

before_install:
  - chmod +x gradlew
 
script: ./gradlew clean assembleDebug

android:
  components:
    - platform-tools
    - tools
    - build-tools-25.0.0
    - android-25
