<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_drawer_header_height"
    android:gravity="bottom"
    android:orientation="vertical">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/background"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription"
        android:id="@+id/imageView"/>

    <ImageView
        android:id="@+id/avatar"
        android:layout_width="64dp"
        android:layout_marginLeft="@dimen/spacing_16"
        android:layout_marginStart="@dimen/spacing_16"
        app:layout_constraintLeft_toLeftOf="parent"
        tools:ignore="ContentDescription"
        app:layout_constraintTop_toTopOf="@+id/imageView"
        android:layout_marginTop="16dp"
        android:layout_height="64dp"/>

    <TextView
        android:id="@+id/username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/spacing_16"
        android:layout_marginStart="@dimen/spacing_16"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/sample.username"
        android:textColor="@color/white"
        android:textSize="14sp"
        app:layout_constraintLeft_toLeftOf="parent"
        tools:text="@string/sample.username"
        app:layout_constraintBottom_toTopOf="@+id/class_progress"
        android:layout_marginBottom="8dp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/class_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/sample.class_progress"
        android:textColor="@color/white"
        android:textSize="14sp"
        tools:text="@string/sample.class_progress"
        app:layout_constraintLeft_toLeftOf="@+id/imageView"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        app:layout_constraintBottom_toBottomOf="@+id/imageView"
        android:layout_marginBottom="16dp"/>

</android.support.constraint.ConstraintLayout>